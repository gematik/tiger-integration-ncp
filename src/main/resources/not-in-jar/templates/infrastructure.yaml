# ${template.warning}
# Template file: ${resources.folder}/${templates.folder}/infrastructure.yaml
epafdv:
  hostname: fdvSim

epaps:
  version: ${epa-ps.version}
  hostname: psSim
  basePath: services

ncpehSim:
  version: ${ncpeh-simulation-api.version}
  hostname: ncpehSim
  basePath: /rest