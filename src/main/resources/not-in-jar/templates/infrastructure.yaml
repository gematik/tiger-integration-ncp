# ${template.warning}
# Template file: ${resources.folder}/${templates.folder}/infrastructure.yaml
epafdv:
  version: ${epa-fdv.version}
  hostname: fdvSim
  basePath: services/rs

epaps:
  version: ${epa-ps.version}
  hostname: psSim
  basePath: services

ncpehSim:
  version: ${ncpeh-simulation-api.version}
  hostname: ncpehSim
  basePath: /rest